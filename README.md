# Automated reports comparing HSCP localities based on 2011 and 2022 data zone boundaries.

To run this report:
- Create a local copy of the repo
- Open `control.R`
- Change the `hscps` variable to the names of the HSCPs you wish to include
- Run the `control.R` script in full
